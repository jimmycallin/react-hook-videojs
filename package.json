{
  "name": "@react-hook-videojs/root",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test:formatting && npm run test:lint && npm run test --workspaces --if-present",
    "test:formatting": "prettier -c .",
    "test:lint": "eslint .",
    "dev": "npm run dev --workspace example & npm run dev --workspace react-hook-videojs",
    "publish": "npm publish --workspace react-hook-videojs"
  },
  "version": "3.0.2"
}

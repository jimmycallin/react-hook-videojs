{
  "name": "@react-hook-videojs/root",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test:formatting && npm run test:lint",
    "dev": "npm run dev --workspace example & npm run dev --workspace react-hook-videojs",
    "test:formatting": "prettier -c .",
    "test:lint": "eslint .",
    "publish": "npm run publish --workspace react-hook-videojs"
  }
}
